# Optional custom domain guide

The archive does not require a custom domain. A future owner may point a subdomain such as `odyssey.example.com` to GitHub Pages, add the domain in repository Pages settings, and then update `site`/`base` in `astro.config.mjs` plus the canonical and sitemap URLs.

No DNS records are changed in CTDE-WEB-01.
