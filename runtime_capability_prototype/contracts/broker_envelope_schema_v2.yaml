$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "urn:ctde:schema:broker-envelope:2"
title: "CTDE Broker Envelope V2 Binding Contract"
type: object
additionalProperties: false
required: [schema_version, assurance_profile_id, run_id, authorization_id, authorization_artifact_sha256, authorization_nonce, consumption_event_id, mint_claim_event_id, capability_preparation_event_id, capability_activation_event_id, capability_id]
properties:
  schema_version: {const: "2.0.0"}
  assurance_profile_id: {type: string}
  run_id: {type: string}
  authorization_id: {type: string}
  authorization_artifact_sha256: {type: string}
  authorization_nonce: {type: string}
  consumption_event_id: {type: string}
  mint_claim_event_id: {type: string}
  capability_preparation_event_id: {type: string}
  capability_activation_event_id: {type: string}
  capability_id: {type: string}
