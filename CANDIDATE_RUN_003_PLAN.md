# Candidate Run 003 Plan

Status: `COMPLETE`

The run sequence is: verify the source and structure-map identities; select Book 1 by strict TEI QName and discriminators; verify the exact ten-card allowlist; perform one current-Codex semantic pass; persist one outline, three sequences, ten beats, provenance hashes, read audit, acceptance evidence, and manifest; then independently rehash and verify every artifact.

Greek content, external model calls, retries, formal database writes, adaptation output, and screenplay output remain outside this run.
